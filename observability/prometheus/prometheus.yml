# Prometheus scrape configuration for InferGrid.

# Phase 0: no targets exist yet; the file is a skeleton so the
# docker-compose service has something to mount. Targets are added
# as components come online (gateway:8080/metrics, etc..
global:
  scrape_interval: 15s

scrape_configs:
  # - job_name: infergrid-gateway
  #   static_configs:
  #     - targets: ["gateway:8080"]